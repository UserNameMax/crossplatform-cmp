---
name: crossplatform-course
description: Контекст курса кроссплатформенной разработки ПИН-252т — сетка занятий, вехи, правила курса, ТЗ и адреса в Notion. Используй, когда спрашивают про устройство курса: что в вехе В1–В4, кто ведёт какую пару, какой такт, где страница занятия, какое ТЗ, а также как общий контекст для скилов crossplatform-lecture и crossplatform-workshop.
---

# Курс кроссплатформенной разработки

Общий контекст. Читается всеми остальными скилами курса.

| Скил | О чём |
|---|---|
| [crossplatform-lecture](../crossplatform-lecture/SKILL.md) | лекция: шкала глубины, гвоздь пары, проверка фактуры замером |
| [crossplatform-workshop](../crossplatform-workshop/SKILL.md) | воркшоп: чекпойнты «делаю X — вижу Y», теги, догон |
| [crossplatform-lab](../crossplatform-lab/SKILL.md) | лабораторная: критерии приёмки, страница ЛР, сдача PR-ом |
| [crossplatform-milestone](../crossplatform-milestone/SKILL.md) | сборка вехи: порядок, согласование стеков, сверка, фиксация |

**Под другой курс меняется только этот файл.** Остальные четыре предметной специфики не
содержат и ссылаются сюда.

**Где живут сами скилы.** Оригинал — `artifacts/skills/` в зонтичном репозитории, под git;
`~/.claude/skills/crossplatform-*` — симлинки на него. Копии нет сознательно, по тому же
правилу, по которому критерии приёмки лежат в контракте, а страницы ЛР на них ссылаются.
Правка метода — это коммит.

## Сетка

Дисциплина — кроссплатформенная разработка, группа ПИН-252т, семестр 01.09.2026 – 26.12.2026.
Ведут двое: **Макс** (Compose Multiplatform / KMP) и **Саша** (Flutter).
Объём 9 лекций / 8 практических / 8 лабораторных, все на всю группу.

Такт темы — две недели: **Лк (вт, онлайн) → 4 дня → Пр (сб, очно) → 7 дней → Лб (сб, онлайн)**.
Семь тематических тактов между вводной и обзорной лекциями.

| Лекция | Дата | Веха | Стек | Ведёт | Гвоздь |
|---|---|---|---|---|---|
| Л0 Введение | 05.09 | — | оба | вдвоём | нет, обзорная |
| Л1 UI и состояние | 15.09 | В1 Каркас | CMP | Макс | позиционная мемоизация и пропуск |
| Л2 UI и состояние | 29.09 | В1 Каркас | Flutter | Саша | три дерева |
| Л3 Данные и асинхронность | 13.10 | В2 Данные | Flutter | Саша | event loop и изоляты |
| Л4 Данные и асинхронность | 27.10 | В2 Данные | CMP | Макс | корутины |
| Л5 Взаимодействие с платформой | 10.11 | В3 Платформа | CMP | Макс | Kotlin/Native |
| Л6 Взаимодействие с платформой | 24.11 | В3 Платформа | Flutter | Саша | Platform Channels |
| Л7 Производительность и тесты | 08.12 | В4 Тесты | Flutter | Саша | джанк по кадрам |
| Л8 Итог и обзор | 22.12 | — | CMP | Макс | нет, стоит после защиты |

| Практика | Дата | Веха | Стек | Ведёт |
|---|---|---|---|---|
| ПЗ1 Каркас | 19.09 | В1 | CMP | Макс |
| ПЗ2 Каркас | 03.10 | В1 | Flutter | Саша |
| ПЗ3 Данные | 17.10 | В2 | Flutter | Саша |
| ПЗ4 Данные | 31.10 | В2 | CMP | Макс |
| ПЗ5 Платформа | 14.11 | В3 | CMP | Макс |
| ПЗ6 Платформа | 28.11 | В3 | Flutter | Саша |
| ПЗ7 Профилирование | 12.12 | В4 | оба | вдвоём |
| ПЗ8 Итоговое | 26.12 | — | оба | вдвоём |

Практические идут **очно по субботам, 11:35–13:05**. Лекции и лабораторные — онлайн.

Целевые таргеты: **Android, web, JVM desktop**. iOS — бонус, без macOS студент не соберёт.

## Вехи

- **В1 Каркас** — навигация и тема на уровне пользования, экран-список на моках, локализация
- **В2 Данные** — сеть, сериализация, пагинация, состояния экрана, асинхронность
- **В3 Персистентность и платформа** — локальный стор, офлайн, **адаптив**, **ресурсы через три
  таргета**, **навигация вглубь: стек записей, deep links, web-адреса**, платформенное API,
  сборка трёх таргетов
- **В4 Тесты и замеры** — одна общая ЛР на оба стека плюс защита

Навигация, тема, адаптив и ресурсы живут **в двух вехах**, не в одной: первые две нужны в В1
буквально, вторые две в В1 нечем мотивировать — там один таргет и один экран на моках.

Чекпойнты вех: 10.10 (В1), 07.11 (В2), 05.12 (В3).

## Парность

Чередование: В1 и В3 начинаются с CMP, В2 — с Flutter. Кто идёт вторым, в выигрыше.

**Мостики к парному занятию обязательны.** Два вида, оба нужны:

- **расхождение** — одну задачу стеки решают по-разному (идентичность узла: позиция вызова
  против типа с ключом)
- **совпадение** — стеки пришли к одному решению (схема измерения layout совпадает почти
  дословно). Это тоже результат сравнения, и его надо назвать вслух, иначе курс читается как
  «везде по-разному»

**Не подменять сравнение стеков сравнением подходов.** Если у одного маршрутная навигация, а у
другого компонентная, в декабре сравнятся философии навигации, а не CMP с Flutter.

## Правила курса

**Единый контракт.** Одна форма приложения и одинаковые критерии приёмки на каждую веху для
обоих стеков. Предметная область у каждого своя, но один и тот же студент пишет одно и то же
своё приложение дважды — на CMP и на Flutter. Без этого получатся два разных проекта, а не
сравнение.

**Ведущий таргет очных воркшопов — desktop.** Одинаково для обоих стеков. Он стартует за
секунды, не требует эмулятора и работает на любой ОС; Android-эмулятор на очной паре съедает
три-пять минут у каждого плюс разброс версий. И это вопрос сравнимости: если CMP вести на
desktop, а Flutter на мобилке, в декабре сравнятся условия запуска, а не стеки. Остальные
таргеты проверяются точечно, одним чекпойнтом ближе к концу.

**Строк в коде нет.** Любая подпись интерфейса лежит в ресурсах, паритет локалей проверяется
скриптом. Требование действует и на преподавательские стенды.

**Приёмка формулируется как «делаю X — вижу Y».** Не «реализовано»: реализованное нечем
принимать.

**Проверять запуском, а не чтением.** При подготовке Л1 и ПЗ1 **четыре** уверенных утверждения
оказались ложными, и все четыре поймал запуск. Последнее — «отдай ViewModel в экран целиком, и он
перестанет быть skippable»: проба 23.08 показала, что функция с параметром-ViewModel остаётся
`restartable skippable`, хотя тип выведен как `Unstable`. Со strong skipping пропускаемость есть у
всех restartable-функций; нестабильный параметр меняет не её, а правило сравнения — по ссылке
вместо `equals`. Если замер расходится с конспектом — правится конспект.

**Чекпойнт вехи.** Веха сдана, когда сданы обе части, а не «сдал CMP».

**Приём асинхронный.** ЛР сдаётся PR-ом к дню своей лабораторной пары; сама пара идёт онлайн
под консультацию и выборочную защиту.

**Дневник различий.** По 5 строк после каждой ЛР: где проще, где дороже, где пришлось лезть
в натив. К ЛР7 у каждого 6 записей — сравнительный отчёт собирается из накопленного.

**Compose Navigation 3 обязательна** (не Decompose, не Voyager). Уточнено 23.08: у CMP
**экран — объект**, а стек — обычный список объектов в состоянии; графа маршрутов и
контроллера нет. У Flutter go_router: маршрут — строка-путь, адресная строка из коробки.
**Это расхождение моделей называется вслух на обеих лекциях и идёт в сравнительный отчёт**,
а не маскируется. Следствие для В3: адрес страницы на CMP связывается со стеком руками.

**Базовая вёрстка** (`Column`, `Row`, `Box`, `Modifier`, `LazyColumn`) показывается **на
ПЗ1**, коротко и по ходу, а не выдаётся чтением на ЛР0 и не добавляется отдельным демо
в шаблон: голосом на онлайн-лекции вёрстка не передаётся.

**iOS** — бонус сверх программы, не опция в требованиях.

**Вне области курса:** CI/CD и публикация в сторы — только на уровне D0 со ссылками.

## ТЗ и данные

**Форма фиксирована, домен у каждого свой** (решено 23.08.2026, против исходного «одно ТЗ на
всех»). Студент выбирает предметную область и источник данных сам, они утверждаются на ЛР0
12.09 и до конца семестра не меняются.

Форма — **каталог**, из Web-трека Академии: список с пагинацией → деталь со связанными
элементами → избранное → поиск → i18n и тёмная тема. Игра, калькулятор, чат, редактор не
подойдут: критерии написаны под каталог и на них не лягут.

**Покедекс на PokéAPI — эталон, а не задание.** На нём стоят воркшопы и демо лекций: нужна
предметка, к которой у всех одинаковый доступ и которую можно показывать с экрана. В
лабораторных он не засчитывается.

Фильтр источника (полностью — в Едином контракте, там же команда проверки): HTTPS,
`access-control-allow-origin` на данных **и отдельно на картинках** — и CMP/Wasm, и Flutter
web грузят изображения запросом, а не тегом `<img>`, — без ключа, серверная пагинация,
не меньше 200 записей, деталь со связями, живой прямо сейчас.

Запасные, прогнанные 23.08.2026: `rickandmortyapi`, `tvmaze`, `openlibrary`, `nobelprize`,
`gbif`, `thecocktaildb`, `dummyjson`. **Не прошли:** оба API Академии (CORS, `ts`+`hash`,
referrer-whitelist), Metropolitan Museum (у картинок нет `ACAO`), Art Institute (IIIF отдаёт
403 даже с браузерным UA), Jikan (два раза подряд 504).

Четыре ограничения, которые всплывут почти у любого источника, — сказать вслух на ЛР0:

1. **Русского языка в данных нет.** Требование вехи — «интерфейс ru/en, **данные en**».
   Локализуется интерфейс, а не содержимое каталога
2. **Поиск может молча игнорироваться.** У PokéAPI `?q=pika` отдаёт **HTTP 200** и полный
   список с начала. Проверять данные, а не код ответа; при отсутствии серверного поиска —
   локальный фильтр по дампу
3. **В списке обычно только имя и ссылка** — деталь и картинку приходится догружать или
   выводить из id
4. **Деталь бывает жирной** — у PokéAPI 265 КиБ (замер 23.08.2026). Урезать нечем, зато это
   материал для замеров В4
## Notion

| Что | Адрес |
|---|---|
| База «Занятия» | `0b8dcf83fdc64683bd3065db1eaa1de6` |
| Источник данных базы | `3df4fa5f-65d4-4963-8fa5-b2534f997e6d` |
| Материалы курса | `3c4c8889-e4c8-81f9-9124-cc30f2d197d4` |
| Единый контракт | `3c4c8889-e4c8-818c-b5d5-c7ef7eb8c8aa` |
| Вехи | `3c4c8889-e4c8-8134-9d05-eb7373e7de43` |
| KMP | `3c4c8889-e4c8-8175-bdc8-c6aa4eca91cd` |
| Flutter | `3c4c8889-e4c8-8196-b9cb-e0632ed34855` |
| **Эталон лекции — Л1** | `3c4c8889-e4c8-8148-81fd-d0c0c92aee41` |
| **Эталон воркшопа — ПЗ1** | `3c4c8889-e4c8-8186-a212-c3a3df5148f3` |

Свойства строки занятия: `Занятие` (title), `Тип` (Лекция/Воркшоп/Лаборатория), `Веха`,
`Дата`, `Стек` (CMP/Flutter/оба), `Ведёт` (Макс/Саша/вдвоём), `Готовность`
(Не начато/В работе/Готово), `Формат` (очно/онлайн).

Строка занятия — это сборка. Подстраницы — её части.

### Грабли Notion

Стоили времени, повторять не надо.

- **Подстраница при создании приклеивается в конец родителя.** Перенести её — одним вызовом
  `update_content` с двумя правками: убрать тег `<page url=...>` снизу и вставить в нужное
  место. Отдельным добавлением тега не выйдет, вернётся ошибка
- В `update_content` поля называются **`old_str` и `new_str`**
- Дата пишется как **`date:Дата:start`**
- Крупные payload обрезаются на середине и валятся с ошибкой парсинга JSON — **резать на
  партии**
- Перед первой записью прочитать `notion://docs/enhanced-markdown-spec`
- Заголовки уровней глубины на страницу идут словами, а не как D1/D2/D3

## Репозитории

Два репозитория на GitHub (аккаунт `UserNameMax`) плюс внутренние заметки вне git, всё
в `/Users/Max/Documents/кросплатформа`:

| Путь | Что | Отдаётся студентам |
|---|---|---|
| `artifacts` → [`crossplatform-cmp`](https://github.com/UserNameMax/crossplatform-cmp) | зонтичный: стенд Л1 в `v1/`, шаблон ПЗ1 подмодулем | да |
| `artifacts/workshop` → [`pokedex-cmp`](https://github.com/UserNameMax/pokedex-cmp) | шаблон ПЗ1 «Покедекс»: `main` = старт, теги `w1-cp1…cp5`, ветка `workshop/w1-end` = финал | да |
| `artifacts/v1` | демо-стенд Л1, девять демо на четырёх таргетах — внутри зонтичного, своего репозитория больше нет | да |
| корень курса | `course-plan-unified.md`, `WORKSHOP-SPEC-*.md`, `api-contract/` — **внутренние заметки, вне git** | нет |

**Почему подмодуль, а не одна история** (решено 23.08.2026). Студент клонирует шаблон
напрямую и не тащит 75 МБ лекционного видео; чекпойнты остаются отдельной историей, которую
можно форкнуть. Зонтичный репозиторий фиксирует шаблон коммитом, а не веткой, — видно, какое
состояние было актуальным.

**Как сдают студенты** (решено 23.08.2026). Своё приложение в своём репозитории, по одному
на стек; репозитории заводятся на ЛР0 12.09. Проект создаётся **визардом**
`kmp.jetbrains.com` (Android + iOS на Compose + desktop + web на Compose, тесты выключены —
возвращаются в В4), а не форком шаблона: завести кроссплатформенный проект — навык вехи, а
не накладной расход. Ветки `develop` и `feature/lab-N_*`, PR в `develop` своего репозитория,
дедлайн — день своей лабораторной пары.

Форк `pokedex-cmp` нужен только тем, кто хочет унести с воркшопа теги догона. На приёмку
лабораторной шаблон не влияет вовсе: **код с пары в лабу не переносится**.

**Чекпойнты и финал выложены сразу** (решено 23.08.2026, против исходного «не пушить
`w1-end` до конца пары»): теги — это механика догона, и на паре они должны уже лежать
в клоне студента.

Спецификации воркшопов лежат **вне** репозитория студентов сознательно: сценарий, риски и
разбор граблей им до пары не нужны.

`.gitignore` шаблона: `build/`, `.gradle/`, `.kotlin/`, `.idea/`, `kotlin-js-store/`,
`xcuserdata/`, `DerivedData/`, `.DS_Store` и обязательно **`local.properties`**.
`gradle-wrapper.jar` наоборот остаётся: без него `./gradlew` не заведётся у клонирующего.

### Открытые вопросы по репозиториям

- `artifacts/v1/media` весит 75 МБ и лежит в истории как есть — и так же запушено. Каждая пересъёмка ляжет туда
  целиком и навсегда. Либо Git LFS, либо не коммитить медиа — решить до второй съёмки
- Заглушки `DemoItems.kt` в стенде Л1 нейтральные: домен выбран, стенд стоит пересобрать на
  покемонах до 15.09
